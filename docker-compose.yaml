version: '3.8'
services:
  db:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=hmodulith_db'
      - 'POSTGRES_PASSWORD=hmodulithapp'
      - 'POSTGRES_USER=hmodulith'
    ports:
      - '5434:5432'
